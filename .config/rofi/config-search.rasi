@import "~/.config/rofi/config.rasi" 

/* ---- Window ---- */
window {
	width: 35%;
	height: 10%;
}

/* ---- Inputbar ---- */
inputbar {
    enabled: true;
    padding: 1px;
    margin: 2px;
}

/* ---- Mainbox ---- */
mainbox {
	padding: 2px;
    children: [ "entry"];
}

/* ---- Entry ---- */ 
entry {
	placeholder: "🔎 Google Search";
    placeholder-color:inherit;
}

